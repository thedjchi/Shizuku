package moe.shizuku.manager.core.models.preferences

enum class StartMode {
    PC,
    WADB,
    ROOT
}

enum class Theme {
    LIGHT,
    DARK,
    SYSTEM
}

enum class UpdateChannel {
    STABLE,
    BETA
}